# Step 2: Set up Jupyter Notebook in Coder Python

If you followed [Step 1](/book/class_setup/001_running_jobs.md#mounting-folders), you should have the following folders in your Coder Python:

```{figure} ../figures/class_setup/step2-folders.png
---
name: folders in coder python
---
Ignore the .script things - these are created by UCloud!
```

## Adding a New Notebook
I recommend creating a folder called `nbs` (short for "notebooks") in your `nlp` folder by right clicking the sidebar. Add then a file with the extension `.ipynb` e.g., `class1.ipynb`

```{figure} ../figures/class_setup/step2-nbs-folder.png
---
name: folders in coder python
---
```

> You can ALSO drag a notebook from your computer onto the sidebar and in the `nlp` folder (e.g., a template)

## Setting up the Notebook
Throughout the class exercises there will be sections where you read code, and other times where you need to code yourself. I suggest creating an empty notebook. You should then copy-paste throuhgout the readings (if needed) or just when doing a `hands-on` exercise.

### Notebook Template
I have also created a template (skeleton) of how I believe a notebook could look lik at this stage (feel free to freestyle at this point).
